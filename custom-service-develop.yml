spring:
  application:
    name: custom-service

server:
  port: 9000

management:
  server:
    port: 7000
  endpoints:
    web:
      base-path: /manage
      exposure:
        include: "*"

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7500/eureka/

custom:
  prop: "development custom prop (changed)"
